# log-01：第一版可用记录

日期：2026-09-21

## 版本定位

第一版保留最小可用的 Python + Playwright 实现：

- 使用项目专属浏览器配置访问语雀，登录状态可复用。
- 展开并滚动虚拟目录，按目录顺序采集知识库文档。
- 逐篇打开文档，清理正文并保存 `说明.md`。
- 点击附件卡片上的真实下载按钮，用 `expect_download()` 保存文件。
- 单篇或单附件失败时写入 `failed.txt`，继续处理后续文档。

## 验收结果

目标知识库：`https://foundationml.yuque.com/foundationml/seminar`

冒烟验收：处理目录前 10 篇，10 篇成功、0 篇失败；保存 11 个附件（10 PDF + 1 PPTX）和 10 份 `说明.md`。重复运行时 11 个附件均按已存在跳过，未重复下载。

全量验收：目录采集到 199 篇文档；199 份 `说明.md` 均已保存；101 篇文档无错误完成；98 篇因至少一个附件下载失败而标记失败；95 个附件新下载，11 个附件因已存在跳过。

## 已知问题

主要失败原因不是访问或 Markdown 保存，而是部分旧 PDF 附件卡片没有页面下载按钮。当前实现依赖 `ne-card-local-doc-btn-download` 触发真实下载，因此这类附件会超时或因未适配卡片类型失败。

V1 不尝试解析私有接口或 CDN 地址，也不静默忽略失败附件。相关文档仍保留 Markdown 和失败记录，便于后续单独补齐。

## 保留文件

- `main.py`：命令行入口、浏览器连接和串行调度。
- `yuque.py`：语雀 DOM selector、目录遍历、正文清理、Markdown 保存和附件下载。
- `login_chrome.ps1`：本机手动登录和兼容模式启动辅助脚本。
- `requirements.txt`：依赖版本。
- `test_yuque.py`：Markdown 清理与文件名处理的单元测试。
